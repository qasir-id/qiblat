import { colorTheme } from '../variables';

const styleAppBar = {
  root: {
    height: 44,
    minWidth: 156,
    borderRadius: 4,
    textTransform: 'capitalize',
    fontWeight: 600,
  },
};

export default styleAppBar;
