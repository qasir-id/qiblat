import { colorTheme } from '../variables';

const styleButton = {
  root: {
    height: 44,
    borderRadius: 10,
    textTransform: 'capitalize',
    fontWeight: 600,
  },
};

export default styleButton;
